<!DOCTYPE html>
<html lang="{{ config.language }}">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title | safe if title else config.title | safe }}</title>
    <meta name="description" content="{{ description or config.description }}" />
    <link rel="apple-touch-icon" sizes="180x180" href="/icons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/icons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/icons/favicon-16x16.png">
    <script type="module" src="/js/main.ts"></script>
  </head>
  <body class="{% if config.colorPreference == 'auto' %}bg-white dark:bg-black{% elif config.colorPreference == 'dark' %}bg-black{% else %}bg-white{% endif %}">
    {{ content | safe }}

    {% block js %}{% endblock %}
  </body>
</html>
